document.addEventListener("DOMContentLoaded", () => {
  //essa função é executada antes de tudo, ela é executada antes de carregar o html da tela e tal

  //essa tela só deve ser acessível se o usário estiver logado

  //pegue o loggedUser do localStorage e verifique se ele está em users (também do localStorage), se as informações não
  //estiverem conforme o esperado o usuário deverá ser redirecionado para a tela de login (usar windows.location.href("") )

  window.onload();
});
